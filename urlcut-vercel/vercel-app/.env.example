# Adicione estas variáveis no painel do Vercel (Settings → Environment Variables)
# O KV_URL, KV_REST_API_URL e KV_REST_API_TOKEN são gerados automaticamente
# ao conectar o Vercel KV no painel. Só adicione o BASE_URL manualmente:

BASE_URL=https://seu-projeto.vercel.app
